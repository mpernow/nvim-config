Repo to contain my neovim setup.

Some packages need to be installed separately:

- `ripgrep` (for fuzzy find)
- `lazygit`
- `tree-sitter-cli` (for `Comment`)
- `nerdfonts`
- `wl-clipboard`

Other that are generally installed anyway:

- `make`
- `gcc`
- `unzip`
- `npm`
- `python3-venv`
